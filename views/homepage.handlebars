{{> user}}
<h1>Homepage</h1>
{{#each posts}}
  <div>
    <h2>{{this.title}}</h2>
    <p>{{this.content}}</p>
    <p>Posted by: {{this.user.username}}</p>
    <p>On: {{this.createdAt}}</p>
  </div>
{{/each}}
